language: groovy
jdk:
  - oraclejdk11

sudo: false

install: true

script:
  - bin/unit-tests.sh

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/